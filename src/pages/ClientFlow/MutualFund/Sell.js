import React from "react";
import { withRouter } from "react-router";
import Sell from "components/clientFlow/MutualFund/Sell";

const SellPage = () => {
  return <Sell />;
};

export default withRouter(SellPage);
