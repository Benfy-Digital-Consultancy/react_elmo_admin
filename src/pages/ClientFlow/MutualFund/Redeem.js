import React from "react";
import { withRouter } from "react-router";
import Redeem from "components/clientFlow/MutualFund/Redeem";

const RedeemPage = () => {
  return <Redeem />;
};

export default withRouter(RedeemPage);
