import React from "react";
import { withRouter } from "react-router";
import { SipComp } from "../../../components/clientFlow/SipDetails/index";

const Sip = () => {
  return <SipComp />;
};

export default withRouter(Sip);
