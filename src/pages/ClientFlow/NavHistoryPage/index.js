import React from "react";
import { withRouter } from "react-router";
import NavHistory from "../../../components/clientFlow/MutualFund/NavHistory/NavHistory";

const NavHistoryPage = () => {
  return <NavHistory />;
};

export default withRouter(NavHistoryPage);
